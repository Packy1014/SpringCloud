spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Packy1014/ConfigServer
          username: packy1014@163.com
          password: Qaz1234
          timeout: 5
          #默认git分支
          default-label: master
server:
  port: 9100
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka